apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-mise-cache
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {app.kubernetes.io/name: mise-cache, app.kubernetes.io/instance: {{ .Release.Name }}}
  template:
    metadata:
      labels: {app.kubernetes.io/name: mise-cache, app.kubernetes.io/instance: {{ .Release.Name }}}
      annotations: {{ toYaml .Values.podAnnotations | nindent 8 }}
    spec:
      serviceAccountName: {{ default (printf "%s-mise-cache" .Release.Name) .Values.serviceAccount.name }}
      securityContext: {runAsNonRoot: true, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: mise-cache
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext: {allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: {drop: ["ALL"]}}
          ports: [{name: http, containerPort: 8080}]
          env:
            - {name: MISE_CACHE_STORAGE, value: {{ .Values.config.storage | quote }}}
            - {name: MISE_CACHE_DATABASE_URL, value: {{ required "config.databaseUrl is required" .Values.config.databaseUrl | quote }}}
            - {name: MISE_CACHE_S3_BUCKET, value: {{ required "config.s3Bucket is required" .Values.config.s3Bucket | quote }}}
            - {name: MISE_CACHE_S3_PREFIX, value: {{ .Values.config.s3Prefix | quote }}}
            - {name: MISE_CACHE_S3_REGION, value: {{ .Values.config.s3Region | quote }}}
            - {name: MISE_CACHE_S3_ENDPOINT, value: {{ .Values.config.s3Endpoint | quote }}}
            - {name: MISE_CACHE_S3_PATH_STYLE, value: {{ .Values.config.s3PathStyle | quote }}}
            - {name: MISE_CACHE_MAX_BLOB_BYTES, value: {{ .Values.config.maxBlobBytes | quote }}}
            - name: MISE_CACHE_TOKENS_JSON
              valueFrom: {secretKeyRef: {name: {{ .Values.secret.existingSecret }}, key: {{ .Values.secret.tokensKey }}}}
          readinessProbe: {httpGet: {path: /v1/status, port: http}, initialDelaySeconds: 2}
          livenessProbe: {httpGet: {path: /v1/status, port: http}, initialDelaySeconds: 5}
          resources: {{ toYaml .Values.resources | nindent 12 }}
          volumeMounts: [{name: tmp, mountPath: /tmp}]
      volumes: [{name: tmp, emptyDir: {}}]

